# Web3 行情监控工具进度

## v0.1.0  2026-05-26

### 已完成

- 初始化 `pnpm workspace`，拆分为 `apps/web` 和 `apps/server`。
- 前端使用 `Vite + React + TypeScript + Ant Design 5 + Pro Components + Zustand + axios + echarts-for-react + Sass`。
- 后端使用 `Fastify + TypeScript + SQLite + ws + zod + decimal.js`。
- 建立统一交易所适配器接口，当前 OKX 支持公共行情订阅和模拟下单，Binance 预留行情查询和模拟下单。
- 建立 SQLite 三张核心表：`monitor_rules`、`trigger_events`、`order_records`。
- 实现监控规则新增、启停、删除、列表。
- 实现策略扫描，按规则检测市场价与目标价。
- 实现触发事件推送，前端通过 WebSocket 接收。
- 实现触发确认后模拟下单，并写入订单记录。
- 增加 `.env.example` 和 `README.md` 说明配置项与启动方式。

### 已确认决策

- 第一版使用 SQLite，后续多人或服务端部署时再迁移 PostgreSQL。
- 第一版默认模拟下单，真实下单需要显式开启总开关并补齐签名和风控。
- 行情数据只放内存，规则、触发事件和订单记录落库。
- 业务层通过 `ExchangeAdapter` 兼容 OKX 和 Binance，避免交易所字段进入策略层。

### 待办

- 补充 OKX 真实下单签名、余额校验和订单状态查询。
- 补充 Binance WebSocket 行情订阅和真实下单签名。
- 增加交易规则校验，例如最小数量、价格精度和最小成交额。
- 增加私有订单推送，用于跟踪部分成交、完全成交和撤单。
- 增加规则编辑能力。
- 增加审计日志导出能力。

### 已知坑位

- 市价买入在不同交易所对 `baseQuantity` 和 `quoteAmount` 的支持不同，后续真实下单前必须在 Adapter 内单独处理。
- WebSocket 行情断线重连已经预留，但 Binance 第一版未接入持续订阅。
- 当前前端确认下单时未做二次价格弹窗展示，后续真实交易前需要补齐。

## v0.1.1  2026-05-26

### 已完成

- 修复 SQLite 可选字段写入时缺少命名参数的问题，统一将空字段写入 `null`。
- 将规则弹窗的 `destroyOnClose` 更新为 Ant Design 5 当前推荐的 `destroyOnHidden`。
- 完成前后端 TypeScript 类型检查。
- 完成后端健康检查、前端页面访问检查、规则新增和删除接口闭环检查。

## v0.1.2  2026-05-26

### 已完成

- 修复前端行情推送在开发环境通过 Vite 代理 `/ws` 时可能误报连接异常的问题。
- 新增 `VITE_WS_BASE_URL` 配置，开发环境默认直连 `ws://localhost:3101/ws`。
- 增加行情推送自动重连，只有连续连接失败时才提示后端服务异常。

### 已确认决策

- 公共行情订阅和本地 WebSocket 推送不需要交易所 API Key。
- API Key 只用于后续真实下单、余额查询和私有订单状态订阅。

## v0.1.3  2026-05-26

### 已完成

- 新增 `docs/用户使用手册.md`，覆盖启动、配置、规则创建、触发确认、模拟下单、常见问题和安全提醒。
- 在 `README.md` 增加用户手册入口。

## v0.2.0  2026-05-26

### 已完成

- 完成第二版阶段 2.1 的规则编辑能力，新增 `PUT /api/rules/:id`。
- `monitor_rules` 增加 `runtime_status` 和 `last_error_message`，启动时自动兼容旧 SQLite 文件。
- 策略扫描会写入运行中、已暂停、已达上限和异常状态。
- 前端规则列表增加运行状态、最近检测时间、最近错误提示和编辑弹窗。

### 已确认决策

- 规则编辑不会清空历史触发次数，避免误删用户已有运行记录。
- 编辑规则后运行状态重置为待运行或已暂停，最近错误清空，便于观察新配置效果。

### 待办

- 进入第二版阶段 2.2 后，需要补审计日志基础能力。
- 后续需要把下单前预览和风控校验接入确认弹窗。

## v0.2.1  2026-05-26

### 已完成

- 新增 `EXCHANGE_HTTP_PROXY` 配置，解决 Node 后端无法通过本机代理访问交易所的问题。
- 后端交易所 REST 请求改用 `undici`，支持 HTTP 代理。
- 后端交易所请求错误信息增加 DNS、连接重置和代理提示。
- 后端配置读取同时支持 `apps/server/.env` 和项目根目录 `.env`。
- 修复布尔环境变量解析，避免字符串 `false` 被误判为 true。
- 规则页增加 5 秒自动刷新，便于观察持续检测状态。
- 用户手册补充运行状态异常和代理配置排查说明。

### 已确认决策

- 公共行情异常优先通过后端代理配置解决，不要求用户填写 API Key。
- WebSocket 代理支持留到后续 Binance 行情和 OKX 私有推送阶段继续完善。

## v0.2.2  2026-05-26

### 已完成

- 策略扫描增加运行锁，避免上一轮未结束时重复扫描。
- 触发生成前增加同规则 `pending` 事件检查，避免重复生成待确认触发。
- 新增 `PATCH /api/triggers/:id/ignore`，支持忽略不需要下单的触发事件。
- 前端触发确认页增加忽略按钮。
- 前端确认下单失败时展示后端返回的明确错误信息。
- 已将历史重复的 pending 触发标记为 ignored，保留一条 confirmed 订单链路。

### 已确认决策

- 重复 pending 触发不自动删除，通过 ignored 状态保留审计痕迹。
- 已处理触发重复确认时后端明确拒绝，防止重复生成模拟订单。

## v0.2.3  2026-05-26

### 已完成

- 将行情刷新和触发判断拆开，启用规则即使达到触发上限也会继续刷新最新行情。
- 策略扫描通过 REST 补偿行情后会广播 `ticker.updated`，保障总览页能收到价格点。
- 前端首次加载 `/api/tickers` 后会生成初始曲线点，避免总览页长期空白。

### 已确认决策

- 当前先用 REST 代理保证行情看板可用，OKX WebSocket 代理支持后续单独完善。

## v0.2.4  2026-05-26

### 已完成

- 新增总览行情固定币种：`BTC-USDT`、`ETH-USDT`、`SOL-USDT`、`DOGE-USDT`、`OKB-USDT`、`BNB-USDT`。
- 新增 `/api/market/overview`，返回价格、24 小时涨跌幅、24 小时成交量和市值预留字段。
- 总览页实时价格曲线增加币种下拉筛选。
- 最新行情改为表格，展示价格、涨跌、24 小时成交量和市值预留。

### 已确认决策

- 市值字段当前显示 `待接入`，后续单独接 CoinGecko 或 CoinMarketCap。
- 当前总览行情使用 OKX ticker 数据，优先保证和交易监控同源。

## v0.2.5  2026-05-26

### 已完成

- 新增 OKX K 线查询能力，使用 `/api/v5/market/candles`。
- 新增 `GET /api/market/candles?symbol=BTC-USDT&bar=5m`。
- 总览页价格图表改为最近 24 小时 5m K 线收盘价走势。
- 图表样式调整为黄色折线、渐变面积、时间轴和美元 tooltip。
- 币种下拉切换时会重新拉取对应币种最近 24 小时数据。

### 已确认决策

- 图表采用 5m 粒度，288 个点覆盖最近 24 小时，兼顾性能和视觉密度。

## v0.2.6  2026-05-26

### 已完成

- 总览页最新行情从 Ant Table 调整为行情榜单布局。
- 为 BTC、ETH、SOL、DOGE、OKB、BNB 增加币种图标和全称展示。
- 最新行情展示列调整为名称、价格、24 小时涨跌、24h 成交量、市值和操作。
- 操作列增加走势快捷按钮，可切换上方图表币种。

### 已确认决策

- BNB 图标使用 Binance 静态资源 `https://bin.bnbstatic.com/static/assets/logos/BNB.png`。
- 市值仍保持 `待接入`，等待后续第三方市值数据源。

## v0.2.7  2026-05-26

### 已完成

- 修复 Binance 静态资源图标 403 问题。
- 将 BTC、ETH、SOL、DOGE、BNB 图标从 CoinGecko 下载到本地。
- 将 OKB 图标从 OKX 下载到本地。
- 前端最新行情图标改为引用 `/coin-icons/*.png` 本地静态资源。

### 已确认决策

- 行情图标全部本地化，避免第三方 CDN 防盗链、403 或网络异常影响页面展示。

## v0.2.8  2026-05-26

### 已完成

- 总览页最新行情从自定义 grid 布局改为 `ProTable`。
- 通过 `ProColumns.render` 保留币种图标、价格副文本、涨跌颜色和操作按钮。
- 清理不再需要的自定义行情 grid 样式。

### 已确认决策

- 项目中能使用 Ant Design 或 Pro Components 的场景优先使用组件，减少自定义布局维护成本。

## v0.2.9  2026-05-26

### 已完成

- 前端入口 `App.tsx` 完成规范化重构。
- 新增集中路由配置 `src/router/routes.tsx`，页面组件使用 `React.lazy` 懒加载。
- 新增 `PageFallback` 作为路由 Suspense 加载态。
- 新增 `ErrorBoundary` 捕获页面渲染异常。
- 启动数据加载抽离为 `useBootstrapTrading`。
- 实时行情推送抽离为 `useRealtimeTrading`。

### 已确认决策

- `App.tsx` 只保留应用级组合逻辑，不直接维护页面 import、数据初始化细节和 WebSocket 事件细节。
- 后端规范化拆分后续单独处理，避免一次重构范围过大。

## v0.2.10  2026-05-26

### 已完成

- 新增后端启动组装模块 `apps/server/src/bootstrap/app.ts`。
- 将 Fastify 创建、CORS 注册、数据库初始化、仓储实例、服务实例、API 路由注册和 WebSocket 绑定集中到 `createServerRuntime`。
- 将 `main.ts` 收敛为服务监听、策略启动和退出信号处理入口。
- 增加关闭保护，避免 SIGINT 和 SIGTERM 重复触发时重复关闭服务资源。
- 修复策略扫描公共行情刷新异常会向外抛出的问题，交易所临时返回错误时服务进程会继续运行，并把错误写入规则运行状态。
- 公共行情刷新失败不会阻断单条规则检测，规则自身的最新价刷新仍会继续执行。

### 已确认决策

- 本次只做后端入口规范化拆分，不拆分具体业务路由，控制改动范围。
- 关闭顺序为先停止策略扫描，再关闭 HTTP 服务，最后关闭 SQLite，避免退出期间继续写库。
- 策略扫描的公共行情刷新失败按运行异常处理，保留服务可用性，具体错误通过规则列表展示，同时继续执行规则级检测。

### 待办

- 后续可以继续将 `api.routes.ts` 按 health、market、rules、triggers、orders 模块拆分。
- 下一阶段建议进入审计日志基础能力，补齐触发、确认、忽略、下单失败和策略异常记录。

## v0.2.11  2026-05-26

### 已完成

- 总览页点击最新行情里的查看走势后，会清空旧 K 线并按当前币种重新请求 `/api/market/candles`。
- 实时价格曲线标题增加当前交易对，便于确认当前展示的币种。
- K 线查询失败时增加页面空状态和消息提示，避免用户误判为按钮无效。
- 将总览页 ECharts 配置和最新行情表格列改为缓存计算，降低实时行情刷新时的重复渲染成本。
- 统计卡片改为订阅计数值，减少无关数组变化引起的重渲染。

### 已确认决策

- 市值字段继续保持 `待接入`，本次不新增第三方市值接口和依赖。
- 当前性能优化先处理浏览器端图表重复更新，后续需要继续处理后端行情订阅重复连接问题。

### 待办

- 接入 CoinGecko 或 CoinMarketCap 获取市值数据。
- 优化后端 `refreshSubscriptions`，避免策略扫描每秒重复重建交易所 WebSocket 行情连接。

## v0.2.12  2026-05-26

### 已完成

- 实时价格曲线默认从 `5m` 切换为 `1m`。
- 后端 `/api/market/candles` 改为返回最近 24 小时 K 线，`1m` 周期返回 1440 个点。
- OKX K 线查询支持 `after` 分页，解决单次最多只能拉取部分 K 线的问题。
- `MarketService` 增加 K 线短缓存，当前缓存 60 秒，避免频繁切换币种时重复请求交易所。
- K 线查询参数改为 `marketCandlesQuerySchema` 校验，保持接口入参结构化。

### 已确认决策

- 本次只做 1m K 线和短缓存，不引入新依赖。
- K 线缓存放在后端内存中，适合当前本地单机原型阶段。

### 待办

- 继续优化后端行情订阅，避免策略扫描每秒重复调用交易所 WebSocket 订阅入口。
- Rules 页面轮询需要拆成只刷新规则数据，避免每 5 秒拉全量数据。
- 后续接入市值数据源时需要增加第三方接口缓存和限流。

## v0.2.13  2026-05-26

### 已完成

- 修复暗色侧边栏菜单悬浮时颜色异常的问题。
- 菜单项链接颜色改为继承菜单项颜色，避免 Link 默认色影响暗色主题。
- 选中菜单项悬浮时保留蓝色高亮态，未选中菜单项悬浮时使用浅色透明背景。

### 已确认决策

- 本次只修改 `DashboardLayout.module.scss`，不改菜单结构和路由逻辑。

## v0.2.14  2026-05-26

### 已完成

- `tradingStore` 新增 `refreshRules`，只请求 `/rules` 并更新规则列表。
- 规则页 5 秒轮询从 `refreshAll` 改为 `refreshRules`，避免重复请求触发、订单、ticker 和总览行情。
- 规则新增、编辑、启停、删除后的刷新范围收敛为规则列表。
- 新增 `rulesLoading`，规则页加载态不再依赖全局 `loading`。

### 已确认决策

- 全局启动仍保留 `refreshAll` 初始化全量数据。
- 规则页只关心规则运行状态、最近检测时间和最近错误，轮询只刷新 `/rules`。

### 待办

- 触发确认页和订单记录页后续也可以拆成局部刷新，减少全局状态更新。

## v0.2.15  2026-05-26

### 已完成

- `tradingStore` 新增 `refreshTriggers` 和 `refreshOrders`，分别只请求 `/triggers` 与 `/orders`。
- 触发确认页刷新范围从 `refreshAll` 收敛为局部刷新。
- 确认模拟下单后只刷新触发事件和订单记录，忽略触发后只刷新触发事件。
- 订单记录页手动刷新改为只请求订单列表。
- 规则页、触发确认页和订单记录页的 `ProTable` columns 改为 `useMemo` 缓存，减少无关重算。
- 新增 `triggersLoading` 和 `ordersLoading`，触发页与订单页加载态不再依赖全局加载状态。

### 已确认决策

- 四个路由页面继续共享同一个交易 store，但页面级刷新必须按业务范围收敛。
- `refreshAll` 只保留给应用启动初始化使用，不再作为列表页日常刷新入口。

### 待办

- 后续可继续拆分首页统计数据选择器，进一步降低实时行情推送时的重渲染范围。

## v0.2.16  2026-05-26

### 已完成

- 新增 `audit_logs` 审计日志表，记录级别、动作、关联实体、规则 ID、触发 ID、订单 ID、摘要和结构化详情。
- 新增 `AuditLogRepository` 和 `AuditLogService`，统一写入和查询审计日志。
- 新增 `GET /api/audit-logs?limit=100`，最多返回 500 条。
- 策略触发生成时写入 `trigger.created`。
- 确认触发和模拟订单提交成功时写入 `trigger.confirmed` 与 `order.submitted`。
- 忽略触发时写入 `trigger.ignored`。
- 确认下单失败时写入 `order.failed`。
- 策略异常时写入 `strategy.error`，并按规则和错误内容做 60 秒去重，避免异常持续发生时刷屏写库。
- 前端补充 `AuditLog` 类型和 `tradingApi.getAuditLogs` 查询入口，方便后续审计日志页面接入。

### 已确认决策

- 审计日志先落 SQLite，适配当前本地单机原型。
- 本次先补后端基础能力和前端 API 入口，审计日志页面后续单独做。
- 策略异常日志需要去重，避免交易所短时异常导致本地数据库快速膨胀。

### 待办

- 新增审计日志页面和菜单入口。
- 后续真实下单、风控、订单同步需要继续复用审计日志服务。

## v0.2.17  2026-05-26

### 已完成

- 前端新增 `decimal.js` 依赖。
- 后端新增 `dayjs` 依赖。
- 后端开发依赖新增 `pino-pretty`。
- 触发确认页的确认弹窗增加目标价、触发价、数量、预估金额、下单方向和订单类型展示。
- 前端预估金额使用 `decimal.js` 计算，避免普通 `number` 直接计算造成精度风险。
- 前端弹窗明确提示预估信息只用于展示，最终计算以后端校验结果为准。
- 后端 Fastify 在开发环境启用 `pino-pretty` 美化日志，生产环境保留 JSON 日志。
- 后端启动日志使用 `dayjs` 输出本地格式化启动时间，接口实体时间仍保持 ISO 字符串。

### 已确认决策

- 前端只做下单信息预估展示，不作为最终下单依据。
- 有基础币数量时，预估金额按 `目标价 * 基础币数量` 计算。
- 有计价币金额时，数量展示计价币金额，预估金额同计价币金额。
- 生产环境日志继续保留结构化 JSON，便于后续日志采集。

### 待办

- 后续需要补后端下单前预览接口，将真实交易规则、余额、风控和滑点校验结果返回给前端。

## v0.2.18  2026-05-26

### 已完成

- 规则新增和编辑从居中弹窗 `ModalForm` 改为右侧抽屉 `DrawerForm`。
- 抽出 `RuleDrawerForm`，统一新增和编辑表单逻辑。
- 规则表单字段按监控条件、下单计划、风控与运行分组。
- Binance 选项标注为预留，降低误操作预期。
- 交易对输入保存前统一转大写。
- 订单类型为市价时隐藏限价价格，提交时清空旧的 `limitPrice`。
- 订单类型为限价时要求填写限价价格。
- 计价币金额和基础币数量增加前端互斥补充校验，至少填写一个。

### 已确认决策

- 规则字段较多，右侧抽屉比居中弹窗更适合后台配置场景。
- 前端校验只做体验优化，最终仍以后端 DTO 校验为准。

### 待办

- 后续接入交易规则后，可以根据交易对动态提示最小数量、价格精度和最小成交额。

## v0.2.19  2026-05-26

### 已完成

- 新增前端行情常量文件 `apps/web/src/constants/market.ts`。
- 将当前支持的 6 个交易对抽成 `SUPPORTED_MARKET_SYMBOLS` 枚举常量。
- 新增 `DEFAULT_MARKET_SYMBOL`、`MARKET_SYMBOL_OPTIONS`、`getCoinSymbol` 和 `getCoinMeta`。
- 总览页币种下拉、默认选中交易对和币种元信息改为使用统一常量。
- 规则页默认交易对改为使用 `DEFAULT_MARKET_SYMBOL`。

### 已确认决策

- 当前前端支持币种仍保持 BTC、ETH、SOL、DOGE、OKB、BNB 六个 USDT 交易对。
- 后续下拉框、列表、币种图标统一复用 `constants/market.ts`，避免散落硬编码。

## v0.2.20  2026-05-26

### 已完成

- OKX 适配器新增 `/api/v5/public/instruments?instType=SPOT` 查询能力。
- 新增 `InstrumentRule` 类型，记录交易对、基础币、计价币、`tickSize`、`lotSize`、`minSize` 和状态。
- 新增 `TradingRuleService`，负责 OKX 交易规则内存缓存和规则入参校验。
- 新增 `GET /api/trading-rules?exchange=okx&symbol=BTC-USDT`，支持按交易对过滤交易规则。
- 规则新增和编辑时接入交易规则校验。
- 校验内容包括交易对是否存在、交易对状态、目标价 tick size、限价 tick size、基础币数量 lot size、最小下单数量、计价币金额按目标价估算后的最小数量。
- 前端补充 `InstrumentRule` 类型和 `tradingApi.getTradingRules`。
- 规则表单交易对从手动输入改为当前支持交易对下拉。

### 已确认决策

- 交易规则当前只接 OKX，Binance 仍保持预留。
- 交易规则先以内存缓存 10 分钟，暂不落 SQLite。
- 当前阶段只做规则同步和校验，不开放真实下单。
- 计价币金额使用目标价估算基础币数量，用于判断是否小于 OKX `minSz`。

### 已验证

- 临时端口验证 `GET /api/trading-rules?exchange=okx&symbol=BTC-USDT` 返回 OKX 规则。
- 临时端口验证 `POST /api/rules` 创建 BTC-USDT 规则成功通过交易规则校验。

### 待办

- 前端规则抽屉后续可以展示当前交易对的 `tickSize`、`lotSize` 和 `minSize` 提示。
- 下单前预览接口需要继续复用交易规则校验结果。

## v0.2.21  2026-05-26

### 已完成

- `MarketService.refreshSubscriptions` 增加交易所级订阅签名缓存。
- 策略扫描每秒调用订阅刷新时，如果交易对集合没有变化，会跳过重新订阅。
- 订阅交易对统一转大写、去重并排序，避免顺序变化导致误判。
- OKX 适配器增加当前 ticker 订阅签名和 handler 缓存。
- OKX WebSocket 在相同 symbols 且连接仍处于 connecting/open 状态时不再重复关闭和重连。
- OKX WebSocket 断线重连时复用当前订阅集合和最新回调。

### 已确认决策

- REST 行情刷新逻辑保持不变，本次只优化 WebSocket 订阅重建。
- 订阅去重优先放在 `MarketService`，适配器侧保留二次保护。

### 待办

- 后续可增加订阅状态查询和连接错误审计，方便页面展示行情连接健康状态。

## v0.2.22  2026-05-26

### 已完成

- 新增审计日志页面 `apps/web/src/pages/AuditLogsPage.tsx`。
- 新增路由 `/audit-logs`，使用懒加载接入。
- 侧边栏菜单新增“审计日志”入口。
- 审计日志页展示级别、动作、消息、关联规则、关联触发、关联订单、详情和时间。
- 支持按日志级别和动作筛选。
- 详情字段通过 tooltip 展示格式化后的 JSON payload。
- 页面内独立加载审计日志，不放入全局交易 store，避免影响行情和交易状态刷新。

### 已确认决策

- 审计日志页面先展示最近 200 条，后续数据量增长后再做分页接口和服务端筛选。
- 审计日志作为排查和观测入口，放入主侧边栏菜单。

### 待办

- 后续可以为审计日志接口增加服务端动作、级别和时间范围筛选。

## v0.2.23  2026-05-26

### 已完成

- 新增 `trading_signals` 表，用于记录规则命中后生成的交易信号。
- 新增 `SignalRepository`，支持信号列表查询、待处理信号查询、创建信号和标记已转换。
- 新增 `SignalService`，负责把策略命中结果先写成信号，再自动转换为待确认触发事件。
- 策略扫描流程从 `规则命中 -> trigger_event` 调整为 `规则命中 -> trading_signal -> trigger_event`。
- 保留人工确认流程，信号转换后仍推送 `trigger.created`，前端触发确认页无需改动。
- 新增审计动作 `signal.created`、`signal.converted` 和 `signal.duplicated`。
- 信号转换为触发事件时继续写入 `trigger.created` 审计日志，保持旧审计链路可查。
- 新增 `GET /api/signals?limit=100`，查询参数使用 DTO schema 校验，最大返回 500 条。
- 前端补充 `TradingSignal` 类型和 `tradingApi.getSignals`，审计日志页面补充信号动作文案。
- 创建信号前会检查同规则是否已有待确认触发，避免触发事件未确认期间重复生成信号并重复增加触发次数。

### 已确认决策

- 当前信号生成后自动转换为触发事件，保持现有半自动确认流程稳定。
- 风控服务后续插入到 `trading_signal -> trigger_event` 之间。
- 当前先做后端信号层和 API 能力，不新增前端信号列表页面。

### 待办

- 新增风控服务，对交易信号进行单笔金额、日次数、行情新鲜度和滑点校验。
- 新增下单前预览接口，返回后端计算的目标价、数量、预估金额、交易规则和风控结果。
- 后续需要信号运营视图时，再新增交易信号页面或在审计日志页增加信号详情入口。

## v0.2.24  2026-05-27

### 已完成

- 按 OKX 官方限频规则优化公共行情请求，`market/tickers`、`market/ticker` 按 IP 限制均为 20 次每 2 秒。
- 总览行情从 6 个单交易对 `market/ticker` 请求改为 1 个 `market/tickers?instType=SPOT` 批量请求，再在本地筛选固定 6 个交易对。
- 总览行情增加 5 秒刷新间隔，策略扫描每秒运行时不会每秒打 OKX REST。
- 单交易对 REST ticker 增加 3 秒最小请求间隔，多个规则命中同一交易对时优先复用缓存。
- 规则检测优先使用 WebSocket 或总览批量行情写入的内存价格，只有行情超过 10 秒未更新时才走 REST 兜底。
- REST 请求遇到 `HTTP 429` 或 OKX `50011` 时进入 30 秒退避，退避期间避免继续请求交易所。
- 总览行情刷新失败只写审计日志，不再把所有启用规则状态改成错误。
- 交易所 HTTP 客户端补充 OKX 业务错误码识别，方便区分 HTTP 层错误和 OKX 业务限频错误。

### 已确认决策

- 当前采用保守限频策略，低于 OKX 官方上限，优先保证本地开发和代理环境稳定。
- 规则状态只反映单条规则自身检测结果，总览看板失败不再污染规则运行状态。
- K 线继续使用 60 秒缓存，暂不调整。

### 待办

- 后续可以增加行情健康状态接口，前端展示 REST 退避剩余时间和 WebSocket 连接状态。
- 后续可以把 OKX 限频参数抽成配置项，方便不同网络环境调整。

## v0.2.25  2026-05-27

### 已完成

- 已结束当前本地前后端开发服务，清理 `D:\demo\web3` 下残留的 Node 开发进程。
- 后端默认端口从 `3001` 调整为 `3101`。
- 前端 Vite 开发端口从 `5173` 调整为 `3102`。
- 前端 `/api` 代理目标调整为 `http://localhost:3101`。
- 前端 `/ws` 代理目标调整为 `ws://localhost:3101`。
- 前端 WebSocket 默认直连地址调整为 `ws://localhost:3101/ws`。
- 本地 `.env` 和 `.env.example` 已同步新端口。
- `README.md` 和 `docs/用户使用手册.md` 已同步默认访问地址。
- Vite 增加 `strictPort: true`，避免 `3102` 被占用时自动切换到其他端口。

### 已确认决策

- 后端固定使用 `3101`，前端固定使用 `3102`。
- 开发环境 WebSocket 继续直连后端，减少 Vite WebSocket 代理不稳定带来的干扰。

### 待办

- 若后续需要多人协作，可以把前后端端口补充到环境变量和启动脚本说明中。

## v0.2.26  2026-05-27

### 已完成

- 新增 `risk_checks` 表，记录每次信号转触发前的风控结果、计价金额、市场价和结构化检查明细。
- 新增 `RiskCheckRepository`，支持写入和查询风控检查记录。
- 新增 `RiskService`，插入到 `trading_signal -> trigger_event` 之间。
- 风控规则当前覆盖单笔最大计价金额、行情新鲜度、真实交易总开关、规则触发上限和同规则待确认触发。
- `trading_signals` 补充 `market_event_time` 和 `simulation_mode`，用于风控判断行情是否过期以及是否属于真实交易。
- 信号通过风控后才会生成 `trigger_event`，风控拒绝时信号会标记为 `rejected`。
- 新增审计动作 `risk.passed` 和 `risk.rejected`，审计日志页面已补充对应文案。
- 新增环境配置 `RISK_MAX_QUOTE_AMOUNT`，默认 `1000`，用于限制单笔最大计价金额。
- 新增环境配置 `RISK_MAX_MARKET_AGE_MS`，默认 `10000`，用于限制行情最大允许延迟。
- 同规则最近一次信号被风控拒绝后，会在规则冷却期内跳过重复生成，避免重复拒绝记录刷屏。
- `README.md` 和 `docs/用户使用手册.md` 已补充风控配置说明。

### 已确认决策

- 风控服务先以内置基础规则实现，暂不新增前端风控配置页面。
- 风控结果先通过审计日志和数据库记录排查，暂不新增风控列表页面。
- 当前风控默认值采用保守本地开发配置，后续可按真实交易需求调整。

### 待办

- 新增下单前预览接口，把后端计算的数量、目标价、预估金额、交易规则和风控结果返回给前端。
- 后续可新增风控配置页面，支持单笔金额、日次数、日金额和允许交易模式配置。
- 后续可为 `risk_checks` 增加查询 API 和前端列表页。

## v0.2.27  2026-05-27

### 已完成

- 新增 `docs/todo.md`，作为后续推进用的勾选式任务清单。
- 已按 `v0.1.x`、`v0.2.x`、`v0.3.x`、`v0.4.x`、`v0.5.x` 整理已完成和待完成事项。
- 已完成任务统一使用 `✅` 标记。
- 未完成任务统一使用 `⬛️` 标记。
- 明确后续每完成一项，需要把状态从 `⬛️` 更新为 `✅`。

### 已确认决策

- `docs/todo.md` 只维护高层任务状态。
- 详细时间线、技术细节和坑位继续维护在 `docs/web3-trading-tool-progress.md`。

### 待办

- 后续完成具体功能时，同步更新 `docs/todo.md` 对应状态。

## v0.2.27-preview  2026-05-27

### 已完成

- 新增 `OrderPreviewService`，提供下单前只读预览能力。
- 新增 `POST /api/orders/preview`，入参为结构化 DTO `{ triggerId: string }`。
- 下单预览会根据触发事件查找关联规则，并由后端统一计算执行参考价、基础币数量、计价币金额和预估成交金额。
- 市价单执行参考价使用当前后端行情价，限价单执行参考价使用规则限价价格。
- 预览接口复用 OKX 交易规则校验，返回交易规则检查摘要。
- 预览接口返回风控摘要，包括触发事件状态、单笔计价金额、行情新鲜度、真实交易总开关和规则触发上限。
- 前端触发确认弹窗打开时请求后端预览接口，不再用前端本地普通展示逻辑作为主要依据。
- 前端确认弹窗展示目标价、触发价、执行参考价、数量、预估金额、滑点、交易规则摘要和风控摘要。
- 当前交易规则或风控预览未通过时，前端确认按钮会禁用。
- `docs/todo.md` 已把 `v0.2.27` 下单前预览接口和触发确认弹窗两项更新为 `✅`。

### 已确认决策

- 预览接口属于查询动作，不写入审计日志，避免用户多次打开弹窗导致日志噪音。
- 确认下单仍走原有 `POST /api/orders/confirm`，后续真实交易前需要在确认接口里补最终强校验。
- 前端只展示后端预览结果，金额和数量计算以后端为准。

### 待办

- 后续在确认下单接口中复用预览和风控逻辑，形成提交前最终校验。
- 后续可以给预览结果增加余额校验和手续费估算。

## v0.2.28  2026-05-27

### 已完成

- 新增 `GET /api/risk-checks?limit=100`，支持查询最近风控检查记录。
- 前端新增风控检查页面 `/risk-checks`。
- 风控检查页面展示状态、交易所、交易对、计价金额、市场价、拒绝原因、关联信号、关联规则和结构化明细。
- 侧边栏新增“风控检查”入口。

### 已确认决策

- 风控检查先按最近 200 条展示，后续数据量增长后再补服务端分页和筛选。

## v0.2.29  2026-05-27

### 已完成

- 新增 `risk_config` 表，用于持久化风控配置。
- 新增 `RiskConfigRepository` 和 `RiskConfigService`。
- 新增 `GET /api/risk-config` 和 `PUT /api/risk-config`。
- 风控配置支持单笔最大计价金额、行情最大允许延迟、每日最大通过风控次数、每日最大通过风控计价金额和交易模式。
- `RiskService` 改为读取数据库风控配置，并增加每日次数和每日金额检查。
- 下单前预览也改为读取数据库风控配置，保持预览和执行前风控口径一致。
- 前端新增风控配置页面 `/risk-config`。
- 侧边栏新增“风控配置”入口。

### 已确认决策

- 风控配置先用 SQLite 单行配置，适合当前本地单机执行体。
- 真实交易仍同时受 `ENABLE_REAL_TRADING` 和风控交易模式控制。

## v0.2.30  2026-05-27

### 已完成

- 前端新增交易信号页面 `/signals`。
- 交易信号页面展示状态、交易所、交易对、方向、订单类型、市场价、目标价、数量或金额、模式、原因、关联规则和生命周期时间。
- 侧边栏新增“交易信号”入口。

### 已确认决策

- 交易信号页面先直接读取 `/api/signals`，后续再按数据量增加服务端筛选。

## v0.2.31  2026-05-27

### 已完成

- 新增 `GET /api/market/health` 行情健康状态接口。
- `MarketService` 增加 REST 退避状态、最近 REST 错误、总览刷新时间、订阅交易对和行情缓存年龄输出。
- 前端新增行情健康页面 `/market-health`。
- 行情健康页面展示 REST 状态、退避结束时间、总览刷新时间、订阅交易对和缓存行情年龄。
- 侧边栏新增“行情健康”入口。

### 已确认决策

- 行情健康先展示后端已有内存状态，不新增独立持久化表。

## v0.2.32  2026-05-27

### 已完成

- 新增 `MarketCapService`，通过 CoinGecko simple price 接口获取美元市值。
- 市值数据增加 60 秒内存缓存，避免频繁请求第三方接口。
- 总览行情刷新时会合并市值数据，失败时不阻断 OKX 行情展示。
- 总览页“最新行情”恢复市值列，缺失时显示 `待接入`。
- `docs/todo.md` 已将 `v0.2.28` 到 `v0.2.32` 待办项全部更新为 `✅`。

### 已确认决策

- 市值数据作为看板增强信息，不参与策略判断和下单风控。
- CoinGecko 请求失败时只保留行情主流程可用，不把规则状态改成错误。

### 待办

- 后续可为市值数据源增加可配置 API Key 和更长缓存周期。

## v0.2.33  2026-05-27

### 已完成

- 调整后续版本路线，把多交易所公共能力提前到 `v0.3.x`。
- 将真实交易前置能力顺延到 `v0.4.x`。
- `docs/todo.md` 已重排后续任务：
  - `v0.3.x` 聚焦 Binance 公共行情、交易规则、K 线、WebSocket 和下单前预览能力。
  - `v0.4.x` 聚焦真实交易前置能力，包括最终校验、余额、签名、真实下单、二次确认和订单状态同步。
- `README.md` 和 `docs/用户使用手册.md` 已补充新的路线说明。
- 明确 Binance 后续接入必须严格以官方 Spot API 文档为准：https://developers.binance.com/docs/binance-spot-api-docs

### 已确认决策

- Binance 公共能力可以提前做，因为不需要 API Key，资金风险低。
- Binance 真实下单不提前做，等公共交易规则、风控和预览链路稳定后再评估。
- Binance 相关实现必须对齐官方文档中的 REST、WebSocket、过滤器和限频规则，不自行猜测接口字段。

### 待办

- 下一步进入 `v0.3.0`，先按 Binance Spot 官方文档梳理字段映射、接口清单和限频策略。

## v0.3.0  2026-05-27

### 已完成

- 按 Binance Spot 官方文档完成公共能力接口梳理，文档地址：https://developers.binance.com/docs/binance-spot-api-docs
- Binance REST 公共接口基地址使用 `https://api.binance.com`。
- Binance WebSocket Streams 使用 `wss://stream.binance.com:9443/stream?streams=...`。
- 明确本阶段只接公共能力和模拟下单，不接 Binance API Key、签名和真实下单。

### 已确认决策

- Binance 公共能力按官方字段实现，不使用猜测字段。
- Binance 真实下单顺延到真实交易阶段再评估。

## v0.3.1  2026-05-27

### 已完成

- Binance 适配器新增 `GET /api/v3/exchangeInfo` 交易规则同步。
- 将 Binance `PRICE_FILTER.tickSize` 映射为统一 `tickSize`。
- 将 Binance `LOT_SIZE.stepSize` 映射为统一 `lotSize`。
- 将 Binance `LOT_SIZE.minQty` 映射为统一 `minSize`。
- 将 Binance `MIN_NOTIONAL.minNotional` 或 `NOTIONAL.minNotional` 映射为统一 `minNotional`。
- 交易规则校验补充最小成交额校验，适配 Binance `MIN_NOTIONAL` 场景。

### 已确认决策

- 统一业务层继续使用 `InstrumentRule`，避免 Binance 过滤器字段直接进入策略层和前端表单。

## v0.3.2  2026-05-27

### 已完成

- Binance 适配器新增 `GET /api/v3/ticker/24hr` 行情快照。
- Binance 适配器新增 `GET /api/v3/klines` K 线查询。
- Binance K 线映射为统一 `MarketCandle`，使用 open time 作为 K 线时间，quote asset volume 作为计价币成交额。
- 后端 `/api/market/overview` 支持 `exchange=binance`。
- 后端 `/api/market/candles` 支持 `exchange=binance`，并按交易所校验支持的交易对。

### 已确认决策

- Binance 总览交易对暂支持 BTC、ETH、SOL、DOGE、BNB 的 USDT 交易对。
- Binance 当前不展示 OKB，因为 Binance 公共交易对不按 OKX 列表猜测。

## v0.3.3  2026-05-27

### 已完成

- Binance 适配器新增 WebSocket ticker 订阅。
- WebSocket stream 使用官方小写 stream 命名，例如 `btcusdt@ticker`。
- 使用 combined stream 地址聚合多个 ticker stream。
- 增加断线重连和重复订阅签名复用，保持和 OKX 适配器一致的业务接口。

### 已确认决策

- 业务层仍通过 `connectTickerStream(symbols, onTicker)` 调用，不暴露 Binance stream 细节。

## v0.3.4  2026-05-27

### 已完成

- 多交易所统一交易对继续使用 `BTC-USDT` 格式。
- Binance REST 和 WebSocket 请求前通过 `toBinanceSymbol` 转换为 `BTCUSDT`。
- Binance 返回交易对通过 `toDisplaySymbol` 转回统一格式。
- 交易规则统一支持价格精度、数量精度、最小数量和最小成交额。
- 下单前预览复用统一交易规则校验，支持 Binance 最小成交额检查。

### 已确认决策

- 多交易所差异收敛在 Adapter 和 TradingRuleService 内，策略层、信号层、风控层继续使用统一领域模型。

## v0.3.5  2026-05-27

### 已完成

- 前端规则表单交易所选择支持 OKX 和 Binance。
- 前端交易对下拉会根据所选交易所切换支持列表。
- 总览页新增交易所切换，支持查看 OKX 或 Binance 公共行情。
- 总览页 K 线查询会按当前交易所请求。
- 行情健康页面新增交易所切换，支持查看 OKX 或 Binance 的 REST 退避、订阅交易对和行情缓存状态。

### 已确认决策

- 前端固定交易对列表按交易所维护，避免把 OKX 特有交易对误用于 Binance。

## v0.3.6  2026-05-27

### 已完成

- 下单前预览接口支持 Binance 规则的统一校验。
- Binance 下单仍保持模拟下单，不触碰 Binance API Key、签名和真实订单接口。
- `docs/todo.md` 已将 `v0.3.x` 多交易所公共能力全部更新为 `✅`。
- `README.md` 和 `docs/用户使用手册.md` 已同步 Binance 公共能力现状。

### 已确认决策

- Binance 真实下单、余额和订单状态同步仍属于 `v0.4.x` 之后的真实交易能力，不在本阶段开放。

### 待办

- 下一步进入 `v0.4.x` 真实交易前置能力前，需要先复盘 OKX 与 Binance 的下单参数差异和 API Key 安全策略。

## v0.3.7  2026-05-27

### 已完成

- 总览页交易对下拉框增加币种图标。
- 规则页交易对下拉框同步复用带图标的交易对选项。
- 将前端行情常量文件从 `market.ts` 调整为 `market.tsx`，用于集中渲染带本地图标的 Select 选项。
- 图标继续使用 `apps/web/public/coin-icons` 下的本地资源。

### 已确认决策

- 币种图标渲染集中在 `constants/market.tsx`，页面不硬编码图标路径。

## v0.3.8  2026-05-27

### 已完成

- 总览页交易所下拉框增加交易所图标。
- 规则页和行情健康页同步复用带图标的交易所选项。
- 新增 `MARKET_EXCHANGE_OPTIONS`，统一维护 OKX 和 Binance 的下拉展示。

### 已确认决策

- OKX 下拉图标暂使用 `OKB.png`，Binance 下拉图标使用 `BNB.png`。
- 交易所图标路径继续集中在 `constants/market.tsx`，页面只引用统一选项常量。

## v0.3.9  2026-05-27

### 已完成

- 规则新增和编辑抽屉中所有可输入、可选择字段补充 tooltip 说明。
- 将表单提示语集中为 `RULE_FIELD_TOOLTIPS`，避免字段 JSX 中重复散落文案。
- 说明文案覆盖交易所、交易对、触发方向、目标价格、检测频率、下单方向、订单类型、金额、数量、限价价格、滑点、冷却、触发次数、下单模式和启用状态。

### 已确认决策

- tooltip 文案参考交易所现货下单常见字段后做业务化精简，前端只做说明展示，最终校验仍以后端为准。

## v0.3.10  2026-05-27

### 已完成

- 规则表单的触发方向文案调整为更明确的上涨触发和下跌触发。
- 触发方向切换时自动带出建议下单方向：
  - 上涨触发默认带出卖出。
  - 下跌触发默认带出买入。
- 下单方向仍允许用户手动修改，兼容突破买入、止损卖出等策略场景。

### 已确认决策

- 联动只在用户主动切换触发方向时生效，打开编辑抽屉时不覆盖已有规则配置。

## v0.3.11  2026-05-27

### 已完成

- 规则表单订单类型选项补充市价和限价的简洁说明。
- `quoteAmount` 表单展示名称按下单方向动态调整：
  - 买入时显示为投入金额。
  - 卖出时显示为卖出金额。
  - 未选择方向时显示为交易金额。
- 最大滑点百分比输入框增加 `%` 后缀，仅影响前端展示，不改变提交值。
- 规则列表的数量或金额展示调整为更直观的 `USDT` 文案。

### 已确认决策

- 后端字段 `quoteAmount`、DTO 和数据库结构保持不变，前端只优化展示文案。

## v0.3.12  2026-05-27

### 已完成

- 规则表单触发方向和订单类型选项增加 `description` 字段。
- 新增 `SelectOptionLabel`，用于统一渲染带描述的下拉选项。
- 下拉选项描述使用 12px、灰色、右对齐样式。
- 已选值通过 `optionLabelProp='title'` 保持只展示主文案，避免描述进入输入框。

### 已确认决策

- 目前仅对触发方向和订单类型使用带描述下拉，其他普通下拉保持简洁展示。

## v0.3.13  2026-05-27

### 已完成

- 规则列表状态切换集成到表格列中。
- `启用状态` 列直接使用 `Switch` 操作单条规则。
- 操作列移除原状态切换入口，仅保留编辑、删除和错误提示。
- 状态切换增加行级 loading，避免重复点击造成重复请求。

### 已确认决策

- `运行状态` 继续展示后端运行态，`启用状态` 只负责用户开关规则。

## v0.3.14  2026-05-27

### 已完成

- 后端新增触发事件、订单记录、交易信号、风控检查和审计日志的单条删除接口。
- 对新增删除接口的路径参数使用 `idParamSchema` 做参数校验。
- 前端触发确认、订单记录、交易信号、风控检查和审计日志列表增加删除操作。
- 删除操作均使用 `Popconfirm` 二次确认，删除成功后刷新当前列表。

### 已确认决策

- 本次删除只删除当前页面对应表中的指定记录，不做自动级联删除，避免误删关联历史。
- 风控配置和行情健康不是历史记录列表，本次不增加删除能力。

## v0.3.15  2026-05-27

### 已完成

- 新增 `docs/量化交易执行全流程.md`。
- 文档按新建规则、策略扫描、交易信号、风控检查、触发确认、下单前预览、订单记录和审计日志梳理完整链路。
- 补充各阶段涉及页面、数据表、关键字段、状态流转和当前阶段说明。

### 已确认决策

- 该文档作为后续理解量化交易执行体主流程的入口文档。

## v0.3.16  2026-05-27

### 已完成

- 左侧菜单按量化交易执行全流程重新排序。
- 主链路页面顺序调整为监控规则、交易信号、风控检查、触发确认、订单记录、审计日志。
- 监控总览、风控配置和行情健康作为辅助页面放在主链路之后。

### 已确认决策

- 菜单顺序优先表达业务执行链路，便于按规则到订单的流程理解系统。

## v0.3.17  2026-05-27

### 已完成

- 9 个主页面的 `PageContainer` 增加 `subTitle`。
- 页面说明按量化交易执行全流程编写，覆盖监控规则、交易信号、风控检查、触发确认、订单记录、审计日志、监控总览、风控配置和行情健康。

### 已确认决策

- `subTitle` 使用一句话说明当前页面职责，避免页面顶部说明过长。

## v0.3.18  2026-05-27

### 已完成

- `docs/量化交易执行全流程.md` 增加“项目特点与交易所内置功能区别”章节。
- 总结本项目区别于欧易、币安内置功能的特点，包括本地策略执行、交易信号层、自定义风控、人工确认、下单前预览、全流程审计、多交易所统一和数据自主。
- 补充本项目与欧易限价委托的关系，以及交易所内置能力和本项目各自更适合的场景。

### 已确认决策

- 项目定位表述为本地量化交易执行体，交易所限价委托只是后续真实下单阶段可提交的一种订单类型。
