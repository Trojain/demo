# Web3 行情监控工具进度

## v0.1.0  2026-05-26

### 已完成

- 初始化 `pnpm workspace`，拆分为 `apps/web` 和 `apps/server`。
- 前端使用 `Vite + React + TypeScript + Ant Design 5 + Pro Components + Zustand + axios + echarts-for-react + Sass`。
- 后端使用 `Fastify + TypeScript + SQLite + ws + zod + decimal.js`。
- 建立统一交易所适配器接口，当前 OKX 支持公共行情订阅和模拟下单，Binance 预留行情查询和模拟下单。
- 建立 SQLite 三张核心表：`monitor_rules`、`trigger_events`、`order_records`。
- 实现监控规则新增、启停、删除、列表。
- 实现策略扫描，按规则检测市场价与目标价。
- 实现触发事件推送，前端通过 WebSocket 接收。
- 实现触发确认后模拟下单，并写入订单记录。
- 增加 `.env.example` 和 `README.md` 说明配置项与启动方式。

### 已确认决策

- 第一版使用 SQLite，后续多人或服务端部署时再迁移 PostgreSQL。
- 第一版默认模拟下单，真实下单需要显式开启总开关并补齐签名和风控。
- 行情数据只放内存，规则、触发事件和订单记录落库。
- 业务层通过 `ExchangeAdapter` 兼容 OKX 和 Binance，避免交易所字段进入策略层。

### 待办

- 补充 OKX 真实下单签名、余额校验和订单状态查询。
- 补充 Binance WebSocket 行情订阅和真实下单签名。
- 增加交易规则校验，例如最小数量、价格精度和最小成交额。
- 增加私有订单推送，用于跟踪部分成交、完全成交和撤单。
- 增加规则编辑能力。
- 增加审计日志导出能力。

### 已知坑位

- 市价买入在不同交易所对 `baseQuantity` 和 `quoteAmount` 的支持不同，后续真实下单前必须在 Adapter 内单独处理。
- WebSocket 行情断线重连已经预留，但 Binance 第一版未接入持续订阅。
- 当前前端确认下单时未做二次价格弹窗展示，后续真实交易前需要补齐。

## v0.1.1  2026-05-26

### 已完成

- 修复 SQLite 可选字段写入时缺少命名参数的问题，统一将空字段写入 `null`。
- 将规则弹窗的 `destroyOnClose` 更新为 Ant Design 5 当前推荐的 `destroyOnHidden`。
- 完成前后端 TypeScript 类型检查。
- 完成后端健康检查、前端页面访问检查、规则新增和删除接口闭环检查。

## v0.1.2  2026-05-26

### 已完成

- 修复前端行情推送在开发环境通过 Vite 代理 `/ws` 时可能误报连接异常的问题。
- 新增 `VITE_WS_BASE_URL` 配置，开发环境默认直连 `ws://localhost:3001/ws`。
- 增加行情推送自动重连，只有连续连接失败时才提示后端服务异常。

### 已确认决策

- 公共行情订阅和本地 WebSocket 推送不需要交易所 API Key。
- API Key 只用于后续真实下单、余额查询和私有订单状态订阅。

## v0.1.3  2026-05-26

### 已完成

- 新增 `docs/用户使用手册.md`，覆盖启动、配置、规则创建、触发确认、模拟下单、常见问题和安全提醒。
- 在 `README.md` 增加用户手册入口。

## v0.2.0  2026-05-26

### 已完成

- 完成第二版阶段 2.1 的规则编辑能力，新增 `PUT /api/rules/:id`。
- `monitor_rules` 增加 `runtime_status` 和 `last_error_message`，启动时自动兼容旧 SQLite 文件。
- 策略扫描会写入运行中、已暂停、已达上限和异常状态。
- 前端规则列表增加运行状态、最近检测时间、最近错误提示和编辑弹窗。

### 已确认决策

- 规则编辑不会清空历史触发次数，避免误删用户已有运行记录。
- 编辑规则后运行状态重置为待运行或已暂停，最近错误清空，便于观察新配置效果。

### 待办

- 进入第二版阶段 2.2 后，需要补审计日志基础能力。
- 后续需要把下单前预览和风控校验接入确认弹窗。

## v0.2.1  2026-05-26

### 已完成

- 新增 `EXCHANGE_HTTP_PROXY` 配置，解决 Node 后端无法通过本机代理访问交易所的问题。
- 后端交易所 REST 请求改用 `undici`，支持 HTTP 代理。
- 后端交易所请求错误信息增加 DNS、连接重置和代理提示。
- 后端配置读取同时支持 `apps/server/.env` 和项目根目录 `.env`。
- 修复布尔环境变量解析，避免字符串 `false` 被误判为 true。
- 规则页增加 5 秒自动刷新，便于观察持续检测状态。
- 用户手册补充运行状态异常和代理配置排查说明。

### 已确认决策

- 公共行情异常优先通过后端代理配置解决，不要求用户填写 API Key。
- WebSocket 代理支持留到后续 Binance 行情和 OKX 私有推送阶段继续完善。

## v0.2.2  2026-05-26

### 已完成

- 策略扫描增加运行锁，避免上一轮未结束时重复扫描。
- 触发生成前增加同规则 `pending` 事件检查，避免重复生成待确认触发。
- 新增 `PATCH /api/triggers/:id/ignore`，支持忽略不需要下单的触发事件。
- 前端触发确认页增加忽略按钮。
- 前端确认下单失败时展示后端返回的明确错误信息。
- 已将历史重复的 pending 触发标记为 ignored，保留一条 confirmed 订单链路。

### 已确认决策

- 重复 pending 触发不自动删除，通过 ignored 状态保留审计痕迹。
- 已处理触发重复确认时后端明确拒绝，防止重复生成模拟订单。

## v0.2.3  2026-05-26

### 已完成

- 将行情刷新和触发判断拆开，启用规则即使达到触发上限也会继续刷新最新行情。
- 策略扫描通过 REST 补偿行情后会广播 `ticker.updated`，保障总览页能收到价格点。
- 前端首次加载 `/api/tickers` 后会生成初始曲线点，避免总览页长期空白。

### 已确认决策

- 当前先用 REST 代理保证行情看板可用，OKX WebSocket 代理支持后续单独完善。

## v0.2.4  2026-05-26

### 已完成

- 新增总览行情固定币种：`BTC-USDT`、`ETH-USDT`、`SOL-USDT`、`DOGE-USDT`、`OKB-USDT`、`BNB-USDT`。
- 新增 `/api/market/overview`，返回价格、24 小时涨跌幅、24 小时成交量和市值预留字段。
- 总览页实时价格曲线增加币种下拉筛选。
- 最新行情改为表格，展示价格、涨跌、24 小时成交量和市值预留。

### 已确认决策

- 市值字段当前显示 `待接入`，后续单独接 CoinGecko 或 CoinMarketCap。
- 当前总览行情使用 OKX ticker 数据，优先保证和交易监控同源。

## v0.2.5  2026-05-26

### 已完成

- 新增 OKX K 线查询能力，使用 `/api/v5/market/candles`。
- 新增 `GET /api/market/candles?symbol=BTC-USDT&bar=5m`。
- 总览页价格图表改为最近 24 小时 5m K 线收盘价走势。
- 图表样式调整为黄色折线、渐变面积、时间轴和美元 tooltip。
- 币种下拉切换时会重新拉取对应币种最近 24 小时数据。

### 已确认决策

- 图表采用 5m 粒度，288 个点覆盖最近 24 小时，兼顾性能和视觉密度。

## v0.2.6  2026-05-26

### 已完成

- 总览页最新行情从 Ant Table 调整为行情榜单布局。
- 为 BTC、ETH、SOL、DOGE、OKB、BNB 增加币种图标和全称展示。
- 最新行情展示列调整为名称、价格、24 小时涨跌、24h 成交量、市值和操作。
- 操作列增加走势快捷按钮，可切换上方图表币种。

### 已确认决策

- BNB 图标使用 Binance 静态资源 `https://bin.bnbstatic.com/static/assets/logos/BNB.png`。
- 市值仍保持 `待接入`，等待后续第三方市值数据源。

## v0.2.7  2026-05-26

### 已完成

- 修复 Binance 静态资源图标 403 问题。
- 将 BTC、ETH、SOL、DOGE、BNB 图标从 CoinGecko 下载到本地。
- 将 OKB 图标从 OKX 下载到本地。
- 前端最新行情图标改为引用 `/coin-icons/*.png` 本地静态资源。

### 已确认决策

- 行情图标全部本地化，避免第三方 CDN 防盗链、403 或网络异常影响页面展示。

## v0.2.8  2026-05-26

### 已完成

- 总览页最新行情从自定义 grid 布局改为 `ProTable`。
- 通过 `ProColumns.render` 保留币种图标、价格副文本、涨跌颜色和操作按钮。
- 清理不再需要的自定义行情 grid 样式。

### 已确认决策

- 项目中能使用 Ant Design 或 Pro Components 的场景优先使用组件，减少自定义布局维护成本。

## v0.2.9  2026-05-26

### 已完成

- 前端入口 `App.tsx` 完成规范化重构。
- 新增集中路由配置 `src/router/routes.tsx`，页面组件使用 `React.lazy` 懒加载。
- 新增 `PageFallback` 作为路由 Suspense 加载态。
- 新增 `ErrorBoundary` 捕获页面渲染异常。
- 启动数据加载抽离为 `useBootstrapTrading`。
- 实时行情推送抽离为 `useRealtimeTrading`。

### 已确认决策

- `App.tsx` 只保留应用级组合逻辑，不直接维护页面 import、数据初始化细节和 WebSocket 事件细节。
- 后端规范化拆分后续单独处理，避免一次重构范围过大。

## v0.2.10  2026-05-26

### 已完成

- 新增后端启动组装模块 `apps/server/src/bootstrap/app.ts`。
- 将 Fastify 创建、CORS 注册、数据库初始化、仓储实例、服务实例、API 路由注册和 WebSocket 绑定集中到 `createServerRuntime`。
- 将 `main.ts` 收敛为服务监听、策略启动和退出信号处理入口。
- 增加关闭保护，避免 SIGINT 和 SIGTERM 重复触发时重复关闭服务资源。
- 修复策略扫描公共行情刷新异常会向外抛出的问题，交易所临时返回错误时服务进程会继续运行，并把错误写入规则运行状态。
- 公共行情刷新失败不会阻断单条规则检测，规则自身的最新价刷新仍会继续执行。

### 已确认决策

- 本次只做后端入口规范化拆分，不拆分具体业务路由，控制改动范围。
- 关闭顺序为先停止策略扫描，再关闭 HTTP 服务，最后关闭 SQLite，避免退出期间继续写库。
- 策略扫描的公共行情刷新失败按运行异常处理，保留服务可用性，具体错误通过规则列表展示，同时继续执行规则级检测。

### 待办

- 后续可以继续将 `api.routes.ts` 按 health、market、rules、triggers、orders 模块拆分。
- 下一阶段建议进入审计日志基础能力，补齐触发、确认、忽略、下单失败和策略异常记录。

## v0.2.11  2026-05-26

### 已完成

- 总览页点击最新行情里的查看走势后，会清空旧 K 线并按当前币种重新请求 `/api/market/candles`。
- 实时价格曲线标题增加当前交易对，便于确认当前展示的币种。
- K 线查询失败时增加页面空状态和消息提示，避免用户误判为按钮无效。
- 将总览页 ECharts 配置和最新行情表格列改为缓存计算，降低实时行情刷新时的重复渲染成本。
- 统计卡片改为订阅计数值，减少无关数组变化引起的重渲染。

### 已确认决策

- 市值字段继续保持 `待接入`，本次不新增第三方市值接口和依赖。
- 当前性能优化先处理浏览器端图表重复更新，后续需要继续处理后端行情订阅重复连接问题。

### 待办

- 接入 CoinGecko 或 CoinMarketCap 获取市值数据。
- 优化后端 `refreshSubscriptions`，避免策略扫描每秒重复重建交易所 WebSocket 行情连接。

## v0.2.12  2026-05-26

### 已完成

- 实时价格曲线默认从 `5m` 切换为 `1m`。
- 后端 `/api/market/candles` 改为返回最近 24 小时 K 线，`1m` 周期返回 1440 个点。
- OKX K 线查询支持 `after` 分页，解决单次最多只能拉取部分 K 线的问题。
- `MarketService` 增加 K 线短缓存，当前缓存 60 秒，避免频繁切换币种时重复请求交易所。
- K 线查询参数改为 `marketCandlesQuerySchema` 校验，保持接口入参结构化。

### 已确认决策

- 本次只做 1m K 线和短缓存，不引入新依赖。
- K 线缓存放在后端内存中，适合当前本地单机原型阶段。

### 待办

- 继续优化后端行情订阅，避免策略扫描每秒重复调用交易所 WebSocket 订阅入口。
- Rules 页面轮询需要拆成只刷新规则数据，避免每 5 秒拉全量数据。
- 后续接入市值数据源时需要增加第三方接口缓存和限流。

## v0.2.13  2026-05-26

### 已完成

- 修复暗色侧边栏菜单悬浮时颜色异常的问题。
- 菜单项链接颜色改为继承菜单项颜色，避免 Link 默认色影响暗色主题。
- 选中菜单项悬浮时保留蓝色高亮态，未选中菜单项悬浮时使用浅色透明背景。

### 已确认决策

- 本次只修改 `DashboardLayout.module.scss`，不改菜单结构和路由逻辑。
