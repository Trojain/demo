# 服务端口，前端默认代理到 http://localhost:3001
PORT=3001

# 前端行情推送地址。开发环境默认直连 ws://localhost:3001/ws
VITE_WS_BASE_URL=ws://localhost:3001/ws

# SQLite 数据文件位置，建议本地开发保留默认值
DATABASE_PATH=./data/web3-trading-tool.db

# 默认交易所，可选 okx 或 binance
DEFAULT_EXCHANGE=okx

# 是否启用真实下单。第一版建议保持 false，只允许模拟下单
ENABLE_REAL_TRADING=false

# 交易所 HTTP 代理。本机无法直连 OKX 或 Binance 时填写，例如 http://127.0.0.1:7897
EXCHANGE_HTTP_PROXY=

# OKX API 配置。真实下单开启时才需要填写
OKX_API_KEY=
OKX_API_SECRET=
OKX_API_PASSPHRASE=
OKX_SIMULATED=true

# Binance API 配置。第一版预留
BINANCE_API_KEY=
BINANCE_API_SECRET=
