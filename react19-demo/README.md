# 🚀 SaaS Admin Classic

> 基于 React 19 + Ant Design 6 + Vite 7 的企业级后台管理系统模板

---

## 📖 新手入门指南

### 第一步：安装依赖

```bash
pnpm install
# 或者 npm install
```

### 第二步：启动开发服务器

```bash
pnpm dev
# 或者 npm run dev
```

浏览器访问：http://localhost:3000

### 第三步：开始开发

- 登录页：`/login`
- 仪表盘：`/`（需要登录）

---

## 🎯 核心特性一览

| 特性                | 说明                  | 收益            |
| ------------------- | --------------------- | --------------- |
| ⚡ **懒加载**       | 页面按需加载          | 首屏速度快 50%+ |
| 🔐 **路由鉴权**     | 自动拦截未登录用户    | 安全可靠        |
| 🎨 **主题定制**     | CSS 变量 + Ant Design | 实时切换主题    |
| 📦 **智能分包**     | Vite 自动代码分割     | 打包体积优化    |
| 🛡️ **TypeScript**   | 严格模式类型检查      | 减少 Bug        |
| 🔥 **Fast Refresh** | 极速热更新            | 开发体验好      |

---

## � 技术栈详解

### 核心框架

- **React 19.2** - 最新稳定版，性能提升 30%
- **Ant Design 6.1** - 企业级 UI 组件库
- **Vite 7.3** - 比 Webpack 快 10 倍

### 开发工具

- **TypeScript 5.7** - 类型安全，智能提示
- **React Router 7** - 路由 + 懒加载
- **Axios** - HTTP 请求封装

### 代码质量

- **ESLint 9** - 代码规范检查
- **Prettier** - 代码格式化
- **Stylelint** - CSS 样式检查
- **Husky + lint-staged** - Git 提交前自动检查

---

## � 目录结构（重点理解）

```
src/
├── 📄 main.tsx                 # 【入口】程序启动文件
├── 📄 App.tsx                  # 【根组件】路由 + 全局初始化
│
├── 📁 components/              # 【通用组件】可复用组件
│   ├── Loading/                # - 加载动画
│   └── AuthGuard/              # - 路由鉴权守卫
│
├── 📁 layouts/                 # 【布局组件】页面框架
│   └── BasicLayout/            # - 侧边栏 + 头部 + 内容区
│
├── 📁 pages/                   # 【页面组件】业务页面
│   ├── Login/                  # - 登录页
│   ├── Dashboard/              # - 仪表盘
│   └── 404/                    # - 404页面
│
├── 📁 router/                  # 【路由配置】所有路由定义
│   └── index.tsx               # - 懒加载 + 嵌套路由
│
├── 📁 services/                # 【API 服务】后端接口封装
│   └── user.ts                 # - 用户相关 API
│
├── 📁 utils/                   # 【工具函数】公共方法
│   ├── request.ts              # - Axios 封装（核心）
│   └── lazyLoad.tsx            # - 懒加载包裹器
│
├── 📁 types/                   # 【类型定义】TypeScript 类型
│   └── api.ts                  # - API 响应类型
│
└── 📁 styles/                  # 【样式文件】全局样式
    ├── variables.scss          # - SCSS 变量
    └── global.scss             # - 全局 CSS
```

---

## 🔥 核心架构解析

### 1️⃣ 请求封装（三大核心）

#### ① 全局单例模式

```typescript
// src/utils/request.ts
const instance = axios.create({...})  // ✅ 全局只创建一次
```

**收益**：TCP 连接复用，性能提升

#### ② 全局注入模式

```typescript
// src/App.tsx
setRequestUi(message, modal, navigate) // ✅ 只初始化一次
```

**收益**：无需每个组件都调用 Hook

#### ③ 防抖 401 弹窗

```typescript
let isRelogging = false  // ✅ 防止弹窗爆炸
if (!isRelogging) { modal.warning(...) }
```

**收益**：10 个并发请求只弹 1 次

---

### 2️⃣ 路由设计（嵌套 + 懒加载）

```typescript
// src/router/index.tsx
const routes = [
  { path: '/login', element: lazyLoad(<Login />) },  // 公开路由
  {
    path: '/',
    element: <AuthGuard><BasicLayout /></AuthGuard>,  // 受保护路由
    children: [
      { index: true, element: <Dashboard /> }  // 默认子路由
    ]
  }
]
```

**关键点**：

- ✅ 懒加载：打包时自动分包
- ✅ AuthGuard：自动拦截未登录用户
- ✅ 嵌套路由：外层布局 + 内层页面

---

### 3️⃣ 样式管理（SCSS + CSS 变量）

```scss
// src/styles/variables.scss
$primary-color: #1677ff;

// src/main.tsx
<ConfigProvider theme={{ cssVar: true }}>
```

**收益**：一处修改，全局生效

---

## 🛠️ 常用命令

```bash
# 开发
pnpm dev              # 启动开发服务器
pnpm build            # 生产构建
pnpm preview          # 预览构建结果

# 代码质量
pnpm lint             # ESLint 检查
pnpm lint:fix         # 自动修复 ESLint 错误
pnpm lint:style       # Stylelint 检查
pnpm format           # Prettier 格式化
pnpm type-check       # TypeScript 类型检查

# 测试
pnpm test             # 运行测试
```

---

## 🎓 学习路径建议

### 新手入门

1. 先跑起来：`pnpm dev`
2. 看懂路由：`src/router/index.tsx`
3. 理解鉴权：`src/components/AuthGuard/index.tsx`
4. 学习请求：`src/utils/request.ts`

### 进阶开发

1. 添加新页面：在 `src/pages/` 创建组件
2. 配置新路由：在 `src/router/index.tsx` 添加路由
3. 封装新 API：在 `src/services/` 创建服务
4. 定义新类型：在 `src/types/` 添加类型

---

## ⚠️ 常见问题

### Q1: 首次运行报错？

**A:** 删除 `node_modules` 和 `pnpm-lock.yaml`，重新 `pnpm install`

### Q2: 登录后白屏？

**A:** 检查浏览器 Console 错误，通常是 token 问题

### Q3: 如何修改主题色？

**A:** 修改 `src/styles/variables.scss` 中的 `$primary-color`

### Q4: 如何添加新页面？

**A:**

1. 在 `src/pages/` 创建组件
2. 在 `src/router/index.tsx` 添加路由
3. 重启开发服务器

---

## 📝 环境变量配置

```bash
# .env.development (开发环境)
VITE_API_BASE_URL=http://localhost:7000

# .env.production (生产环境)
VITE_API_BASE_URL=https://api.production.com
```

---

## 📚 推荐文档

- [React 19 官方文档](https://react.dev)
- [Ant Design 6 文档](https://ant.design)
- [Vite 官方文档](https://vitejs.dev)
- [React Router 文档](https://reactrouter.com)

---

## � License

MIT

---

**⭐ 如果这个项目对你有帮助，欢迎 Star！**
